import React from 'react';

const styles = {
  title: {
    color: '#757575',
    fontSize: '16px',
    fontWeight: 'lighter',
  },
};

const NoAdsFound = () => (
  <center>
    <div style={styles.title}>לא נמצאו מודעות</div>
  </center>
);

export default NoAdsFound;
